#include "TUBES.h"

int main() {

    //kode dibawah cuman buat testing function-functionnya, jangan dihapus
    /*
    ListDokter D;
    newListDokter(D);
    dataDokter k;
    adrDokter L;
    for(int i = 0; i < 3; i++){
        cin >> k.idDokter >> k.namaDokter >> k.spesialisasi;
        L = newNodeDokter(k);
        insertLastDokter(D, L);
    }

    displayDLL(D);
    deleteFirstDokter(D);
    displayDLL(D);
    */
    /*
    ListPasien P;
    newListPasien(P);
    adrPasien kk;
    dataPasien z;
    for(int i = 0; i < 3; i++){
        cin >> z.namaPasien >> z.alergi >> z.riwayatPenyakit >> z.idPasien >> z.umur;
        kk = newNodePasien(z);
        insertLastPasien(P, kk);
    }
    displaySLLPasien(P);
    deleteFirstPasien(P);
    displaySLLPasien(P);
    deleteLastPasien(P);
    displaySLLPasien(P);
     */

    return 0;
}
